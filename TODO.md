# 开发待办

本文件记录尚未完成的工作和取舍依据。已完成的功能写入 `HISTORY.md`，用户使用方法写入 `README.md`。

旧项目补丁只能作为行为参考，不复制其解包后的游戏文件，也不把游戏源码或资源加入本仓库。所有必须修改游戏代码的兼容补丁都应在浏览器内存中作用于用户提供的 ASAR 内容，原归档保持只读。

## 补丁分层

这里的“三层”是职责边界，不是简单的文件覆盖顺序：

```text
浏览器壳核心（运行环境、存储、会话和资源生命周期）
                         │
游戏 app.asar → 用户模组层（列表越靠后优先级越高）
                         │
                         ▼
                  最终分层 VFS 资源
                         │
                         ▼
          游戏兼容档案（Profile）的必要转换
                         │
                         ▼
                    Tyrano 运行
```

### 第一层：浏览器壳核心

负责任何受支持游戏都必须具备、且不能由模组关闭的基础能力：

- ASAR 只读解析、分层 VFS、资源路径解析与 Blob URL 生命周期
- `fetch`、XHR、Worker、DOM、CSS 与媒体资源拦截
- IndexedDB 存档、Electron API 模拟和 Tyrano 启动衔接
- 会话隔离、启动握手、错误报告和资源释放

这部分运行契约已经稳定。源码目录已按 `kernel / profiles / mods / shell / vendor` 五个职责域收拢，后续只在域内维护窄模块，不再重复拆散或进行框架迁移。

### 第二层：游戏兼容档案（Profile）

负责特定《Devil Connection》版本在浏览器中运行所必需的、与原始源码结构相关的内存补丁。当前 APNG 二进制兼容转换属于这一层。

每个新增补丁必须满足：

- 作用于最终 VFS 选中的文件，因此模组覆盖同一路径后仍会接受必要兼容检查
- 指定目标路径、支持的源码特征、预期匹配次数和失败方式
- 源码特征不匹配时明确停止或报告，不进行模糊替换
- 只修改解决问题所需的最小片段，不复制整份 Tyrano 或游戏脚本
- 有独立测试，且在启用和禁用相关模组时进行浏览器验证

只有浏览器无法正确运行原行为时才进入这一层。由于该层位于资源覆盖之后，用户不能靠调整模组顺序绕过它，所以体验优化不能混入这里。

### 第三层：模组

负责可选性能优化、体验调整、功能扩展和特定内容修补：

- 继续使用“本体 → 模组由上到下，后加载覆盖前加载”的规则
- 性能策略应允许用户启用、禁用和调整顺序
- 针对少量具体场景文件的修改优先制作兼容模组
- `hook.js` 仍视为受信任代码，不把模组提升为壳核心权限

`dc_safe_apng.asar` 这类惰性解码和缓存回收策略应留在这一层，不并入不可关闭的壳核心。

## 分层机制待办

- [x] 使用 `SessionPreparer` 固定三层交汇顺序：最终本体加模组 VFS → Profile 必要转换 → 壳层浏览器资源准备 → 游戏文档构建；`PlayerController` 只管理会话生命周期。
- [x] 让内存转换后的资源成为当前会话中 `readText()`、`getBlob()` 和 Blob URL 的一致视图，避免后续消费者绕回未转换的 VFS 内容。
- [x] 为现有 APNG 补丁声明补丁 ID、目标路径、必要性、失败方式和应用状态，并严格验证源码匹配次数。
- [x] 将源码收拢为 `kernel / profiles / mods / shell / vendor` 五个职责域，迁移时保持 ASAR、VFS、资源重写、存档和启动模块的既有契约。
- [x] 使用小型声明式 `ProfileRunner` 枚举必要补丁；文本补丁声明严格源码特征及预期次数，二进制补丁声明读取上限、精确大小和 SHA-256，所有补丁均声明目标、失败策略与转换函数。
- [x] 增加只读“兼容性”管理页，显示必要补丁的“已应用、无需转换、不受支持、应用失败”状态及最终资源来源；必要补丁失败时阻止启动并自动打开该页。
- [x] 支持导出不含本地路径、文件句柄、归档内容或源码的兼容性诊断 JSON。
- [x] 让转换后的文本资源延迟到首次实际取用时才创建 Blob URL，并让本地文本 XHR 使用逻辑 `responseURL`，减少手机端大量小对象 URL 的常驻数量。
- [x] 检测 CSS 循环依赖并打断回边，避免样式中残留已撤销的 Blob URL。
- [x] CSS 准备阶段只登记最终分层文本，首次访问时才同步生成实际依赖闭包，避免未使用样式提前实体化其图片和字体 URL。
- [x] 按样式、图片、音频、视频、字体、文本和二进制分类记录对象 URL 的当前值与会话峰值。
- [x] 二进制 API 优先在 iframe realm 中直接生成 `ArrayBuffer`，仅在不兼容时保留复制回退。
- [x] 停用或移除模组时释放同步文本缓存，重新启用时按需重建；读取前统计启用模组文本总量，32 MiB 仅产生非阻断的开发者软提示。
- [x] 为严格源码签名、基础层二进制补丁、已知模组覆盖和未知模组运行时委托增加自动测试，并验证报告保留最终来源层。
- [ ] 使用真实 `LayeredVfs` 与最终模组覆盖组合补充 Profile 集成矩阵；覆盖同路径本体、精确匹配模组、未知模组和不受支持本体，而不是继续扩充孤立的 resolver stub。
- [x] 为 Host 媒体回退、资源就绪层、预加载调度器、会话准备失败和 Resolver 退出增加释放或取消测试。
- [ ] 随剩余补丁逐项完成生命周期审计；当前缺口集中在标题循环 `MediaSource`、APNG 解码缓存和截图/相册异步任务，不再把已经覆盖的 Host 回退重复列为待办。

## 旧项目补丁候选

以下项目来自对旧项目浏览器补丁的审计。实现时只借鉴问题和策略，需重新基于当前 VFS 与兼容层完成。

### 优先引入

- [x] **有界 Tyrano 预加载调度器**：在壳核心的 Tyrano 适配边界包装 `kag.preload()` / `preloadAll()`，限制图片、音频与视频的同时加载和解码数量，合并语义相同的同 URL 在途任务；支持失败放行、30 秒超时、页面退出取消和 DOM 遥测，不做永久资源缓存，也不全局拦截普通 fetch/XHR 或媒体 Range 请求。
- [x] **资源呈现就绪屏障**：作为 Host kernel 浏览器契约，在 Tyrano `image` 标签开始原始插入和淡入前完成预加载及 `decode()`，不再全局隐藏最终图片；既有视频预加载回调只等待和记录，不覆盖可见性、背景、场景推进或 `movie_with_bg` 时序。失败、超时和退出均失败放行。
- [x] **Android Chromium fragmented MP4 恢复**：真机以独立内存 Blob 复现相同 AAC demux 错误，排除 ASAR range/高位 slice/字节缺失。Host kernel 只在受管 MP4/M4V 返回错误码 4 后，对不超过 16 MiB 且严格识别 codec 的 fragmented ISO BMFF 建立一次 MSE 表示；不做 UA sniff、转码、游戏资源转换或永久缓存。
- [x] **静音视频 Android 兼容**：`kiri2.mp4` 和 `effect.mp4` 都是普通非分片 MP4，无法进入 fragmented MSE 回退；其 AAC 轨经解码确认全静音。Game Profile 为两份资源声明独立的严格大小和 SHA-256 签名，仅将各自 `soun` 轨的等长容器类型改为 `free`，不移动 `mdat`、视频 sample 或 chunk offset，不改写磁盘和 ASAR。
- [x] **未知模组 MP4 仅画面兜底**：Host kernel 只在受管 MP4/M4V 真实返回错误码 4 且保声 MSE 不可用或失败后，范围解析非分片 H.264/AAC 结构并生成等长无音轨复合 Blob；不整文件复制、不修改 VFS/ASAR，每个元素最多重试一次，明确警告并随来源生命周期释放。静音视频 Profile 对未知模组同名覆盖记录委托，基础游戏签名未知仍中止。
- [ ] **标题循环视频队列**：串行处理 `SourceBuffer.appendBuffer()`，等待 `updateend` 后再追加；处理 `InvalidStateError`、加载失败、重复切换和退出，并在结束时解除监听、关闭 `MediaSource`、撤销 URL。17.18 MiB 的 `title_main.mp4` 是唯一超过 Host MSE 回退上限的视频，但只有 H.264 轨且由该插件自己的 MSE 路径读取；归入游戏兼容档案，目标行为参考旧项目 `data/others/plugin/title_loop/main.js`。
- [x] **存档持久化一致性**：使用版本化 localStorage 写前日志让同步存档调用在返回前留下可恢复值或删除标记；IndexedDB 串行提交失败时保留日志供重试，重新启动时 fallback 与日志覆盖同名旧数据库值，全量替换的 reset 标记防止删除项复活，且只有覆盖该版本的事务成功后才清理恢复记录。

### 验证后引入

- [ ] **Android 资源压力诊断**：通过调试工具和真机开发者工具统一记录分类对象 URL 峰值、模组文本总量、APNG 的 Worker/Data URL/Image/Canvas 生命周期，以及长 BGM 切换后的 Howler AudioBuffer 回收。最终资源有 12 个超过 16 MiB 的 APNG，完整 RGBA 帧上界约 124–347 MiB；最长 BGM 的双声道 Float32 PCM 理论体积约 108.6 MiB。取得数据后再校准或移除 32 MiB 软提示，并决定是否需要可选 `dc_safe_apng`、流式音频或释放策略；在此之前不增加硬限制、永久缓存或可能撤销仍被引用资源的 LRU。
- [ ] **截图与相册可靠收敛**：先复现截图失败、页面克隆和相册删除问题，再合并处理 `snapSave` 单次结束、无缩略图存档、html2canvas `onclone` 克隆隔离，以及删除记录后的孤立图片数据；优先包装稳定接口，只能依赖源码结构时才进入版本 Profile。
- [ ] **移动端输入提交复位**：执行 Tyrano `[commit]` 后立即并在下一帧再次复位 `#tyrano_base` 滚动位置；先在真实移动浏览器复现输入法导致的画面偏移。
- [ ] **Backlog 初始化**：移除依赖固定 150 ms 的异步探测，改为通过 VFS 明确判断所需文件是否存在；确认不会改变原版初始化顺序。

### 暂缓

- [ ] **WAAPI 资源错误处理**：先确认当前运行时是否仍会因 XHR 或解码失败让 `isLoading` 永久不归零，再设计有超时和失败状态的实现。

### 明确不引入

- 全局永久 Howl 音频缓存：会让长流程内存持续增长。
- 全量资源预取或无上限加载队列：与按需读取 ASAR 的目标冲突。
- 未复现字体阻塞时全局注入 `font-display: swap`：可能重新引入字体闪烁；若只是体验偏好，应由可选模组或设置负责。
- 未测量到具体场景卡顿时修改六处场景预载，或增加 `jump` / `call` 场景预取：本地 ASAR 收益不明，且会扩大解码内存和 Blob URL 生命周期。
- 未复现布局问题时覆盖 Remodal 移动缩放：保留原版行为，出现具体问题后重新建立最小复现。
- 并行加载 Tyrano 核心脚本：容易破坏脚本依赖顺序，收益不足以抵消风险。
- 整份复制 `kag.js`、`kag.tag.js`、`kag.menu.js` 或 `libs.js`：版权、升级和模组覆盖成本过高。
- 旧项目的加载遮罩、点击启动门、`configSave` 覆盖和调试库：当前壳已有对应能力或不属于运行必需功能。
- 为尚未复现的问题引入宽泛 monkey patch：先建立最小复现和回归测试。

## 验收规则

每完成一项补丁，至少确认：

- 不修改、不解包、不上传用户的 ASAR 文件
- 原版与常用模组组合均能启动并到达目标场景
- 补丁目标不匹配时有清晰错误，不静默继续运行损坏代码
- 刷新、重新载入和退出后没有失效 Blob URL 或会话资源残留
- 存档中不出现 Blob URL、VFS 层 ID 或临时对象标识
- 新增自动测试，并执行 `AGENTS.md` 中与改动相关的完整验证
