# 开发待办

本文件记录尚未完成的工作和取舍依据。已完成的功能写入 `HISTORY.md`，用户使用方法写入 `README.md`。

旧项目补丁只能作为行为参考，不复制其解包后的游戏文件，也不把游戏源码或资源加入本仓库。所有必须修改游戏代码的兼容补丁都应在浏览器内存中作用于用户提供的 ASAR 内容，原归档保持只读。

## 补丁分层

这里的“三层”是职责边界，不是简单的文件覆盖顺序：

```text
浏览器壳核心（运行环境、存储、会话和资源生命周期）
                         │
游戏 app.asar → 用户模组层（列表越靠后优先级越高）
                         │
                         ▼
                  最终分层 VFS 资源
                         │
                         ▼
          游戏兼容档案（Profile）的必要转换
                         │
                         ▼
                    Tyrano 运行
```

### 第一层：浏览器壳核心

负责任何受支持游戏都必须具备、且不能由模组关闭的基础能力：

- ASAR 只读解析、分层 VFS、资源路径解析与 Blob URL 生命周期
- `fetch`、XHR、Worker、DOM、CSS 与媒体资源拦截
- IndexedDB 存档、Electron API 模拟和 Tyrano 启动衔接
- 会话隔离、启动握手、错误报告和资源释放

这部分运行契约已经稳定。源码目录已按 `kernel / profiles / mods / shell / vendor` 五个职责域收拢，后续只在域内维护窄模块，不再重复拆散或进行框架迁移。

### 第二层：游戏兼容档案（Profile）

负责特定《Devil Connection》版本在浏览器中运行所必需的、与原始源码结构相关的内存补丁。当前 APNG 二进制兼容转换属于这一层。

每个新增补丁必须满足：

- 作用于最终 VFS 选中的文件，因此模组覆盖同一路径后仍会接受必要兼容检查
- 指定目标路径、支持的源码特征、预期匹配次数和失败方式
- 源码特征不匹配时明确停止或报告，不进行模糊替换
- 只修改解决问题所需的最小片段，不复制整份 Tyrano 或游戏脚本
- 有独立测试，且在启用和禁用相关模组时进行浏览器验证

只有浏览器无法正确运行原行为时才进入这一层。由于该层位于资源覆盖之后，用户不能靠调整模组顺序绕过它，所以体验优化不能混入这里。

### 第三层：模组

负责可选性能优化、体验调整、功能扩展和特定内容修补：

- 继续使用“本体 → 模组由上到下，后加载覆盖前加载”的规则
- 性能策略应允许用户启用、禁用和调整顺序
- 针对少量具体场景文件的修改优先制作兼容模组
- `hook.js` 仍视为受信任代码，不把模组提升为壳核心权限

`dc_safe_apng.asar` 这类惰性解码和缓存回收策略应留在这一层，不并入不可关闭的壳核心。

## 分层机制待办

- [x] 使用 `SessionPreparer` 固定三层交汇顺序：最终本体加模组 VFS → Profile 必要转换 → 壳层浏览器资源准备 → 游戏文档构建；`PlayerController` 只管理会话生命周期。
- [x] 让内存转换后的资源成为当前会话中 `readText()`、`getBlob()` 和 Blob URL 的一致视图，避免后续消费者绕回未转换的 VFS 内容。
- [x] 为现有 APNG 补丁声明补丁 ID、目标路径、必要性、失败方式和应用状态，并严格验证源码匹配次数。
- [x] 将源码收拢为 `kernel / profiles / mods / shell / vendor` 五个职责域，迁移时保持 ASAR、VFS、资源重写、存档和启动模块的既有契约。
- [x] 使用小型声明式 `ProfileRunner` 枚举必要补丁；每项补丁声明目标、严格源码特征、预期次数、失败策略与转换函数。
- [x] 增加只读“兼容性”管理页，显示必要补丁的“已应用、无需转换、不受支持、应用失败”状态及最终资源来源；必要补丁失败时阻止启动并自动打开该页。
- [x] 支持导出不含本地路径、文件句柄、归档内容或源码的兼容性诊断 JSON。
- [ ] 为最终分层资源增加版本/特征测试，覆盖本体文件和被模组覆盖后的文件。
- [ ] 为每个补丁检查 Blob URL、事件监听器、计时器、媒体缓冲区和解码缓存的释放路径。

## 旧项目补丁候选

以下项目来自对旧项目浏览器补丁的审计。实现时只借鉴问题和策略，需重新基于当前 VFS 与兼容层完成。

### 优先引入

- [ ] **有界 Tyrano 预加载调度器**：在壳核心的 Tyrano 适配边界包装 `kag.preload()` / `preloadAll()`，限制图片、音频与视频的同时加载和解码数量，合并同一 URL 的在途任务；必须支持失败放行、超时、会话退出取消和遥测，不做永久资源缓存，也不全局拦截普通 fetch/XHR 或媒体 Range 请求。
- [ ] **标题循环视频队列**：串行处理 `SourceBuffer.appendBuffer()`，等待 `updateend` 后再追加；处理 `InvalidStateError`、加载失败、重复切换和退出，并在结束时解除监听、关闭 `MediaSource`、撤销 URL。归入游戏兼容档案，目标行为参考旧项目 `data/others/plugin/title_loop/main.js`。
- [ ] **存档截图可靠收敛**：保证 `snapSave` 在截图、图片加载或 `toDataURL()` 失败时也只结束一次，并允许无缩略图存档，避免剧情永久卡住。优先通过 Tyrano 适配器包装稳定接口；只能依赖源码结构时再使用版本补丁。
- [ ] **截图克隆隔离**：使用 html2canvas 的 `onclone` 只清理克隆文档中的文件输入或不可序列化状态，不修改正在运行的游戏 DOM。与存档截图可靠收敛一并实现。
- [ ] **手动存档等待持久化**：成功提示应等待 IndexedDB 写入完成；失败时保留可重试数据并显示存储错误。
- [ ] **存储回退一致性**：补强 IndexedDB 失败后的操作记录与恢复合并，避免旧 IndexedDB 数据遮蔽 localStorage 中更新的存档。
- [ ] **相册数据清理**：修复截图失败时的等待状态，并清理被删除记录留下的孤立图片数据。

### 验证后引入

- [ ] **移动端输入提交复位**：执行 Tyrano `[commit]` 后立即并在下一帧再次复位 `#tyrano_base` 滚动位置；先在真实移动浏览器复现输入法导致的画面偏移。
- [ ] **Backlog 初始化**：移除依赖固定 150 ms 的异步探测，改为通过 VFS 明确判断所需文件是否存在；确认不会改变原版初始化顺序。
- [ ] **字体显示策略**：评估对最终 VFS CSS 添加 `font-display: swap` 的收益与闪烁影响。若只是体验偏好，做成可选模组或设置，不进入必要兼容层。
- [ ] **六处场景预载**：逐一验证 `_Devil_BBB.ks`、`_Devil_debirun.ks`、`_kill_muumuu.ks`、`_kill_panpu.ks`、`_omake_debi.ks` 和 `_preview.ks` 的卡顿复现与资源生命周期；需要时制作独立兼容模组，不直接修改本体。

### 暂缓

- [ ] **WAAPI 资源错误处理**：先确认当前运行时是否仍会因 XHR 或解码失败让 `isLoading` 永久不归零，再设计有超时和失败状态的实现。
- [ ] **角色和背景显示前预载**：会改变 `nextOrder` 时序，只在有明确白屏或竞态复现时处理。
- [ ] **`jump` / `call` 场景预取**：本地 ASAR 的收益可能有限，必须先测量命中率、内存和 Blob URL 增长。
- [ ] **Remodal 移动缩放**：仅在当前管理器或游戏模态框出现真实布局问题时引入。

### 明确不引入

- 全局永久 Howl 音频缓存：会让长流程内存持续增长。
- 全量资源预取或无上限加载队列：与按需读取 ASAR 的目标冲突。
- 并行加载 Tyrano 核心脚本：容易破坏脚本依赖顺序，收益不足以抵消风险。
- 整份复制 `kag.js`、`kag.tag.js`、`kag.menu.js` 或 `libs.js`：版权、升级和模组覆盖成本过高。
- 旧项目的加载遮罩、点击启动门、`configSave` 覆盖和调试库：当前壳已有对应能力或不属于运行必需功能。
- 为尚未复现的问题引入宽泛 monkey patch：先建立最小复现和回归测试。

## 验收规则

每完成一项补丁，至少确认：

- 不修改、不解包、不上传用户的 ASAR 文件
- 原版与常用模组组合均能启动并到达目标场景
- 补丁目标不匹配时有清晰错误，不静默继续运行损坏代码
- 刷新、重新载入和退出后没有失效 Blob URL 或会话资源残留
- 存档中不出现 Blob URL、VFS 层 ID 或临时对象标识
- 新增自动测试，并执行 `AGENTS.md` 中与改动相关的完整验证
